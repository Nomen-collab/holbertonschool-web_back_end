/**
 * Returns a Promise.
 * @returns {Promise<any>} A Promise that resolves immediately.
 */
export default function getResponseFromAPI() {
  return new Promise((resolve) => {
    resolve();
  });
}
